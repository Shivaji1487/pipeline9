apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv-v9
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /srv/mysql-v9