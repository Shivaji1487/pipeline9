apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline8-app
  namespace: bank-production-v8
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pipeline8-app
  template:
    metadata:
      labels:
        app: pipeline8-app
    spec:
      initContainers:
      - name: copy-html
        image: sshivaji555/pipeline8Trivy-app:latest
        imagePullPolicy: Always
        command:
          - sh
          - -c
          - cp -r /usr/share/nginx/html/* /pv/
        volumeMounts:
          - name: html-volume
            mountPath: /pv

      containers:
      - name: nginx
        image: sshivaji555/pipeline8Trivy-app:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        volumeMounts:
          - name: html-volume
            mountPath: /usr/share/nginx/html

      volumes:
      - name: html-volume
        persistentVolumeClaim:
          claimName: pipeline7helm-pvc