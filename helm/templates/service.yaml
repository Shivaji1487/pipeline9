apiVersion: v1
kind: Service
metadata:
  name: pipeline7helm-service
  namespace: bank-production
spec:
  type: ClusterIP
  selector:
    app: pipeline7-app
  ports:
    - port: 80
      targetPort: 80