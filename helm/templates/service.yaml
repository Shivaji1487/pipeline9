apiVersion: v1
kind: Service
metadata:
  name: pipeline8Trivy-service
  namespace: bank-production-v8
spec:
  type: ClusterIP
  selector:
    app: pipeline8-app  # <--- MUST MATCH deployment.yaml
  ports:
    - port: 80
      targetPort: 80